name: Build CrashStat APK

on:
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.7'

      - name: Build debug APK
        run: gradle -p crashstat-android assembleDebug --stacktrace

      - name: Rename APK
        run: cp crashstat-android/app/build/outputs/apk/debug/app-debug.apk CrashStat-Mobile.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: CrashStat-Mobile-APK
          path: CrashStat-Mobile.apk
          if-no-files-found: error
          retention-days: 30
